# Bump this and push to master: CI builds the installer and publishes a
# release tagged v<version>, which installed copies update from. A push that
# leaves it alone builds but does not release.
__version__ = "1.0.0"
